# AI Teams v3 デプロイ手順 / 稼働記録（個人利用・出先アクセス / 2026-06）

VPS（ConoHa Windows Server, FX と同居）に **uvicorn 単一プロセス**で同居配信し、
**cloudflared クイックトンネル**で即時公開する個人向け構成。**2026-06-02 に実デプロイ・疎通確認済み**。

## なぜ VPS か（Vercel ではない理由）

バックエンドは **常駐プロセス**が必須でサーバーレス（Vercel）に載らない:
- SSE の長時間ストリーム（討論は数分、floor-open は無期限待機）
- リクエストを越えて生きるバックグラウンドスレッド（producer 完走 / paused 待機）
- インメモリのセッション状態（`SESSIONS` 辞書・スレッド・条件変数）＝**単一ワーカー前提**

→ FastAPI バックエンドは VPS。フロントは**静的書き出しして同じ uvicorn が同居配信**するのが最も簡単
（Node 常駐不要・低RAM・CORS 不要）。

## 実際の構成（2026-06 稼働中）

```
[ブラウザ(出先)] --HTTPS--> [cloudflared quick tunnel (*.trycloudflare.com)]
                                   │ （ポート開放なし・自動HTTPS・SSE素通し）
                              [uvicorn :8000 (VPS, 単一ワーカー)]
                                   ├ /sessions /personas /presets /intake → FastAPI（Bearer 認証）
                                   ├ /health                              → FastAPI（無認証）
                                   └ / , /_next/* , その他                → web/out の静的SPA（無認証で配信）
```

- フロントは `next build`（`output:"export"`）で `web/out/` に純粋な静的ファイルを生成。
- `api/main.py` 末尾で `web/out` があれば `StaticFiles` を `"/"` にマウント。**全 API ルートの後**なので
  `/sessions` 等が優先され、それ以外（`/` や `/_next/*`）は SPA を返す。→ **1プロセス・同一オリジン**。
- フロントの API 呼び出しは `apiUrl()`（`NEXT_PUBLIC_API_BASE=""`＝相対パス）でバックエンド直結。
  **Next.js の rewrite は使わない**（SSE をバッファして「終わってから一括表示」になる既知の罠。実測済み）。
  cloudflared は SSE を素通しする。

## 認証・コストモデル（BYOK = 各自が自分のキーを持参）★現行

共有/公開するので「サーバ所有者のキーが他人に使われる」事故を **BYOK** で根本的に塞ぐ。

- **実 LLM は来訪者が自分の Anthropic キーを使う**。フロントはキーを **ブラウザの localStorage にのみ**
  保存し、毎リクエストの `X-Anthropic-Key` ヘッダで送る。サーバは **保存・ログせず**、その鍵で実 LLM を
  呼ぶ（`AnthropicClient` のメモリ内のみ・セッション終了で消える）。
- サーバは `AI_TEAMS_BYOK=1` のとき **サーバ env キーを来訪者リクエストに一切使わない**。実 LLM 要求で
  キー未提供なら **400**。`mock=true` はキー不要（無料プレビュー）。
- `/health` は BYOK 時 `api_key_set:false` を返し、サーバ鍵の有無を漏らさない（偵察情報を絞る）。
- 公開インスタンスは `AI_TEAMS_READONLY=1` で**編成 CRUD（ペルソナ/プリセットの作成・更新・削除）を 403**。
- レート制限（`/sessions`・`/intake`、IP 毎・固定窓、`CF-Connecting-IP` 優先）＋進行中セッション同時上限
  `AI_TEAMS_MAX_ACTIVE`（paused 無期限残留による DoS 対策、超過 429）。
- 共有時は `AI_TEAMS_API_TOKEN` を**未設定**にして UI を開放（各自キーで使う）。アクセス自体の本人確認
  ゲートが欲しければ **名前付きトンネル＋Cloudflare Access** を別途（後述）。

## 環境変数

`sandbox/ai-teams/.env`（gitignore 済み・VPS にのみ）— **共有/BYOK 構成（現行）**:
```
AI_TEAMS_BYOK=1                   # 各自キー必須。サーバ env キーを来訪者に使わせない
AI_TEAMS_READONLY=1               # 編成 CRUD を 403（来訪者にサーバ上のファイルを触らせない）
AI_TEAMS_MODEL=claude-opus-4-8    # 任意（既定 opus-4-8）
# ANTHROPIC_API_KEY は置かない（BYOK では未使用。残っていても BYOK 時は来訪者に使われない）
# AI_TEAMS_API_TOKEN も未設定（UI 開放。各自キーで使う）
# 任意: AI_TEAMS_MAX_ACTIVE=12 / AI_TEAMS_RATE_MAX=20 / AI_TEAMS_RATE_WINDOW=60
```
`web/.env.local`（ローカルのビルド時のみ・gitignore 済み）:
```
NEXT_PUBLIC_API_BASE=            # 空＝相対パス＝同一オリジン（トークンは焼かない）
```
> 個人専用（非共有）に戻すなら: `.env` から BYOK/READONLY を外し `ANTHROPIC_API_KEY` を置く（従来の
> サーバ鍵運用）。さらに Cloudflare Access でログインゲート。BYOK と Access は併用も可。

## 再現手順

### ローカル（ビルド）
```
cd sandbox/ai-teams/web
npm run build            # → out/（静的SPA。NEXT_PUBLIC_* が埋め込まれる）
```
### VPS へ反映
```
# コードは git（main.py の StaticFiles マウント等）
ssh ... "cd C:\bpr_lab; git pull origin <branch>"
# 静的フロントは out/ を scp（git 管理外）
scp -r web/out  Administrator@<vps>:C:/bpr_lab/sandbox/ai-teams/web/
# .env は scp（チャットにキー/トークンを貼らない）。AI_TEAMS_API_TOKEN を out/ と一致させる
```
### VPS で常駐起動（Task Scheduler / 単一ワーカー）
- Windows では **Task Scheduler** で常駐（ssh 越しの `Start-Process` は silent death する既知の罠 →
  `Start-ScheduledTask` 経由。`ExecutionTimeLimit=0`＝無制限にしないと既定 72H でハードkill）。
- 稼働中タスク:
  - **`AITeams_API`**: `python -m uvicorn api.main:app --port 8000`（WorkingDirectory = ai-teams）
  - **`AITeams_Tunnel`**: `cloudflared.exe tunnel --url http://localhost:8000 --logfile C:\bpr_lab\cloudflared.log`
  - いずれも AtLogOn トリガ / `ExecutionTimeLimit=0` / `RestartCount` 付き。
- `cloudflared.exe` は `C:\bpr_lab\cloudflared.exe`（GitHub releases から取得・git 管理外）。

## クイックトンネル URL について（重要な制約）

- 公開 URL は `https://<ランダム>.trycloudflare.com`。**トンネル再起動（再起動・クラッシュ復帰）ごとに変わる**
  （＝ブックマーク不可）。
- **現在の URL を知る**: VPS の `C:\bpr_lab\cloudflared.log` を見る:
  ```
  ssh ... "powershell -c \"(Select-String C:\bpr_lab\cloudflared.log -Pattern 'https://[a-z0-9-]+\.trycloudflare\.com').Matches.Value | Select -Last 1\""
  ```
- **安定 URL ＋ ログイン認証が欲しくなったら → 名前付きトンネル ＋ Cloudflare Access**（要 Cloudflare ドメイン・
  `cloudflared tunnel login`＝ブラウザ対話）。これで固定ドメイン＋Google/メールのログインゲートになる。

## コスト・運用の要点

1. **BYOK なので実 LLM 課金は各来訪者の自分のキー**。サーバ所有者のキーは来訪者に使われない（致命的リスクを解消）。
2. **mock は無料プレビュー**として残す（キー未入力でも UI と挙動を試せる＝BYOK の「キー入力前」状態）。
3. **DoS 抑止**: レート制限＋進行中同時上限。ただし `CF-Connecting-IP`/`XFF` は偽装余地があり完全ではない
   （多層防御の一枚）。アクセス自体を絞るなら Cloudflare Access。
4. **単一ワーカー固定**（インメモリ状態共有のため `--workers` を付けない）。
5. **環境変数の変更は uvicorn の再起動が必要**（`.env` は import 時に一度だけ読む）。タスク再起動で子 python が
   残りポート 8000 を握ることがあるので、**8000 を Listen する PID を kill してから** `Start-ScheduledTask`
   （FX の python は 8000 を使わないので無関係）。
6. **`.env` を git に乗せない**（gitignore 済み）。**チャットにトークン/キーを貼らない**。

## 動作確認チェックリスト（2026-06-02 BYOK 化・公開URLで全 PASS）

- [x] `GET /health` → `{llm:"byok", api_key_set:false, byok:true}`（サーバ鍵の有無を漏らさない）
- [x] `GET /`（公開URL）→ SPA（`AI COUNCIL`）が同一オリジン配信
- [x] `POST /sessions` 実LLM（mock:false）・キー無し → **400**（来訪者にサーバ鍵を使わせない）
- [x] `POST /sessions` mock:true → 200（無料プレビュー）／ SSE はトンネル越しに逐次配信（既検証）
- [x] `POST /intake` 実LLM・キー無し → 400
- [x] `POST /personas`（書込）→ **403**（`AI_TEAMS_READONLY`）
- [ ] 実キーを入れての実 LLM e2e は各自のブラウザで（キーは localStorage のみ・サーバ非保存）
- [ ] （任意・次段）名前付きトンネル＋Cloudflare Access で安定 URL＋ログインゲート

---
**更新日**: 2026-06-02 ／ 実構成: VPS uvicorn 単一プロセス（静的SPA同居）＋ cloudflared クイックトンネル ＋ Task Scheduler 常駐 ＋ **BYOK（各自キー）**。
旧版（Caddy + next start / token-in-bundle 認証）は本構成に置換。安定URL＋ログインは「名前付きトンネル＋Access」へ拡張。
