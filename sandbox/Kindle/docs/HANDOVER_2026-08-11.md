# ハンドオーバー 2026-08-11（PC再起動による中断）

## ① 30秒サマリ

Kindle自動撮影ツールを v2 に全面リライト完了（Phase 0 バグ修正 + Phase 1 OCR/自動検知）。
pytest 76件・ruff・mypy 全グリーン、対抗レビューの critical 3件も修正・検収済み。
**残るは実機E2Eのみ**: Kindle for PC が起動しない状態だったため、実機での通し撮影が未実施。
PC再起動後、少ページの本で1冊試すところから再開する。

## ② 進捗

- [x] Phase 0: 1ページ目欠落 / 例外時の画像全損 / ウィンドウ誤検出 / キー方式化 — 解消（v2構造で根治）
- [x] Phase 1: 開き方向自動判定（1ページ目検証付き）/ クライアント領域自動撮影 / 差分方式の自動クロップ / OCRテキスト層 / 書名自動命名 / 連続モード
- [x] OCR環境: Tesseract 5.4.0（jpn同梱）winget導入済み。Ghostscript は `--output-type pdf` で不要（実測確認済み）
- [x] 対抗レビュー → critical 3 / high 7 / medium・low 修正適用 → 全チェック再実行で検収
- [ ] **実機E2E（未実施）**: Kindle for PC 実機での通し撮影
- [ ] コミットはこの中断時点で実施（このハンドオーバーと同コミット）

## ③ 確定事実

- 検証コマンド（すべて `sandbox/Kindle` で実行、venv使用）:
  - `./.venv/Scripts/python.exe -m pytest -q` → 76 passed
  - `./.venv/Scripts/python.exe -m ruff check .` / `-m mypy <各モジュール>` → クリーン
  - `runOcr()` 実機貫通確認済み（テキスト層付きPDF生成をsidecarで検証）
- アーキテクチャ: main.py（統括・連続モード）/ capture.py（プローブ・撮影ループ）/ window_manager.py（kindle.exe優先検出）/ postprocess.py（差分方式クロップ）/ ocr.py / pdf_engine.py
- 開き方向判定プロトコル: 1ページ目前提を検証し、栞位置で開くと 'ambiguous' を返して再操作を促す
- 終端検知: 完全一致×3連続（白紙2枚連続では止まらない）
- 既知の制約（README「既知の制約」参照）: プライマリモニタ限定 / 撮影中のウィンドウ移動不可 / EscはKindleにも届く / OCR中はEsc不可
- 目的整理（2026-08-11ヒアリング）: NotebookLM取り込みが主目的。対象は資格学習テキスト+ビジネス書。まとめ買い/1冊都度の両対応。発展希望: 音声概要・横断QA・library還流・試験対策・anki
- GitHub類似調査: 乗り換え先なし。輸入した設計=2段階OCR構想(book-capture)/多形態出力(kindle-ai-export)/N連続終端(kindle2ss)

## ④ 未解決の問い

1. **実機で安定検知（完全一致基準）が成立するか** — Kindleの描画に非決定性があると `waitForStablePage` がタイムアウト警告を頻発する可能性。初回実機実行で確認
2. 終端検知の約20秒待ちが体感で許容範囲か（短縮調整の余地は capture.py の autoCapture 側にあり）
3. 実機のクロップ品質（差分方式が進捗バー等をどこまで含めるか）
4. NotebookLM 取り込み後の検索性・引用品質（OCR精度の実書籍での確認）
5. Phase 2 未着手: テキスト(md)抽出→ `sandbox/タスクマネージャー/library/` 還流、ankiカード生成（サブエージェント方式）、flash-study連携
6. GitHub公開の具体化（ユーザー意向あり。公開時は feedback_oss_extraction_from_monorepo.md の手順を参照）

## ⑤ 再開時の最短ルート

1. Kindle for PC を起動し、**少ページの本**（マンガ以外のリフロー本推奨）を**1ページ目**で開く
2. `cd sandbox/Kindle && ./.venv/Scripts/python.exe main.py` → Enter
3. 確認ポイント: ①開き方向判定が正しいか ②全ページ撮れているか（1ページ目含む） ③Downloads の `{書名}_{日時}.pdf` が検索可能か（Ctrl+Fで本文語を検索） ④クロップ具合
4. 問題なければ本命の資格テキスト/ビジネス書で実運用 → NotebookLM に投入して品質確認
5. 不具合が出たら README のトラブルシューティングと本ファイル④を参照
