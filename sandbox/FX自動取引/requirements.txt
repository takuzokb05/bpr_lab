# FX auto-trading system - dependencies
# NOTE: keep comments ASCII-only. On Windows, pip reads this file with the cp932
# locale; non-ASCII bytes raise UnicodeDecodeError and silently abort the install.

# === Broker API ===
oandapyV20>=0.7.2
MetaTrader5>=5.0.45

# === Data processing ===
pandas>=2.0.0
numpy>=1.24.0

# === Technical indicators ===
pandas-ta>=0.3.14b

# === Signal processing (bear_researcher divergence: scipy.signal.find_peaks) ===
scipy>=1.10.0

# === Backtest ===
Backtesting>=0.3.3

# === HTTP / Telegram notify ===
requests>=2.28.0

# === LLM (SPEC v3 Direct Filter: Claude Sonnet 4.6) ===
anthropic>=0.40.0

# === Env management ===
python-dotenv>=1.0.0

# === Config (pair_config.yaml) ===
PyYAML>=6.0

# === Database: SQLite is in the Python stdlib ===

# === Test ===
pytest>=7.0.0
pytest-cov>=4.0.0

# === Logging: logging is in the Python stdlib ===
